.edit-page {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
  padding-bottom: 190rpx;
  background: #fffdf8;
}

.edit-head {
  display: grid;
  gap: 8rpx;
  padding-top: 8rpx;
}

.form-section {
  display: grid;
  gap: 16rpx;
  padding: 20rpx;
  background: #ffffff;
  border: 1rpx solid #e4ded1;
  border-radius: 8rpx;
}

.field-group {
  display: grid;
  gap: 10rpx;
}

.field-label {
  color: #253027;
  font-size: 25rpx;
  font-weight: 800;
  line-height: 1.25;
}

.field {
  height: 82rpx;
  padding: 0 20rpx;
  color: #253027;
  font-size: 28rpx;
  font-weight: 650;
  background: #f9f7f2;
  border-radius: 8rpx;
}

.compact-row {
  margin: 18rpx 0 0;
}

.cover-panel {
  display: grid;
  gap: 14rpx;
  overflow: hidden;
  background: #ffffff;
  border: 1rpx solid #e4ded1;
  border-radius: 8rpx;
}

.cover {
  width: 100%;
  height: 382rpx;
  background: #e8e0d4;
}

.cover-placeholder {
  display: grid;
  gap: 8rpx;
  min-height: 382rpx;
  align-content: center;
  padding: 26rpx;
  background:
    linear-gradient(135deg, rgba(255, 253, 248, 0.5), rgba(229, 240, 231, 0.45)),
    #e8e0d4;
}

.cover-title {
  color: #253027;
  font-size: 30rpx;
  font-weight: 850;
  line-height: 1.3;
}

.cover-actions {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
  gap: 12rpx;
  padding: 0 16rpx 16rpx;
}

.cover-actions .action {
  height: 76rpx;
  font-size: 26rpx;
}

.photo-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14rpx;
}

.photo-summary-item {
  overflow: hidden;
  background: #ffffff;
  border: 1rpx solid #e4ded1;
  border-radius: 8rpx;
}

.photo-thumb {
  width: 100%;
  height: 150rpx;
  background: #e8e0d4;
}

.photo-copy {
  display: grid;
  gap: 4rpx;
  padding: 14rpx;
}

.photo-summary-name {
  color: #2f6f4c;
  font-size: 26rpx;
  font-weight: 800;
}

.photo-summary-meta {
  color: #697366;
  font-size: 23rpx;
}

.item-preview {
  display: grid;
  gap: 10rpx;
}

.item-row {
  display: grid;
  grid-template-columns: 44rpx minmax(0, 1fr);
  gap: 12rpx;
  align-items: center;
  min-height: 70rpx;
  padding: 12rpx 16rpx;
  background: #ffffff;
  border: 1rpx solid #e4ded1;
  border-radius: 8rpx;
}

.item-index {
  width: 40rpx;
  height: 40rpx;
  color: #ffffff;
  font-size: 22rpx;
  font-weight: 850;
  line-height: 40rpx;
  text-align: center;
  background: #2f6f4b;
  border-radius: 50%;
}

.item-name {
  min-width: 0;
  overflow: hidden;
  color: #253027;
  font-size: 27rpx;
  font-weight: 780;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.more-row {
  padding: 16rpx 18rpx;
  color: #697366;
  font-size: 25rpx;
  text-align: center;
  background: #f7f3ea;
  border-radius: 8rpx;
}

.save-actions {
  position: fixed;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 35;
  display: grid;
  grid-template-columns: 0.9fr 1.15fr;
  gap: 16rpx;
  padding: 18rpx 36rpx calc(18rpx + env(safe-area-inset-bottom));
  background: rgba(255, 253, 248, 0.96);
  border-top: 1rpx solid rgba(219, 212, 197, 0.82);
  backdrop-filter: blur(14rpx);
}

.save-actions .primary-button,
.save-actions .soft-button {
  height: 88rpx;
  font-size: 28rpx;
  white-space: nowrap;
}
